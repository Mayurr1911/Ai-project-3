# Transportation Traffic Prediction

This project uses a Random Forest Regressor to predict traffic volume based on time, weather, and other factors.

## Requirements
- Python 3.x
- Install dependencies: `pip install -r requirements.txt`

## Usage
Run the model.py script to train and save the model.

## Dataset
Uses synthetic traffic data for demonstration.
